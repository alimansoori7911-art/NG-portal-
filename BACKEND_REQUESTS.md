# درخواست‌های فرانت از بک‌اند

بر اساس `openapi (14).json` — به‌روزشده با جواب‌های بک‌اند (۱۴۰۵/۰۶/۰۸)

> **وضعیت فرانت:** هر ۱۳۰ اندپوینت اسپک وصل شده و هر ۶۵ اندپوینت فلو
> پیاده شده. هر چیزی که در ادامه می‌آید یعنی صفحه‌ای که ساخته شده
> ولی داده ندارد.

---

## ✅ قول داده شده — منتظر اسپک بعدی

| # | مورد | جواب بک‌اند |
|---|---|---|
| ۸ | `GET /orders/{id}/invoices` | اضافه می‌کنم |
| ۹ | `PATCH /auth/me` (ویرایش پروفایل کاربر) | اوکی |
| ۱۰ | ویرایش اطلاعات کاربر توسط ادمین | اوکی |

با آمدنشان، فرم‌های مربوطه وصل می‌شوند.

## ⏸️ فاز توسعه — تصمیم گرفته شد

| مورد | یادداشت |
|---|---|
| ریلیز نوت | فعلاً به‌شکل نوتیف معمولی |
| تراکنش‌های مالی | «مدیریت مالی» فعلاً فقط همین تب را دارد |
| لایسنس | ناقص نوشته شده؛ فعلاً لایسنس‌سرور UI خودش را دارد |
| برچسب VIP | — |

**سؤال درباره‌ی لایسنس:** چون لایسنس‌سرور رابط کاربری خودش را دارد،
آیا اصلاً لازم است تب لایسنس در پرتال بماند؟ اگر نه، حذفش می‌کنیم و
یک صفحه‌ی کمتر روی داده‌ی نمونه می‌ماند.

## ❌ حذف‌شده — وجود نخواهد داشت

**لاگ کاربر عادی** — بک‌اند تأیید کرد چنین چیزی نداریم و قرار هم
نیست بیاید.

✅ **تصمیم گرفته شد:** آیتم «مدیریت LOG» و صفحه‌اش از داشبورد حذف
شدند، هرچند در فیگما بودند. دلیل: نه اندپوینتی در اسپک دارد، نه در
فلو هست، و کاربر را به صفحه‌ای می‌برد که هرگز داده نخواهد داشت.
تنها مسیر لاگ `/audit/` است که مخصوص ادمین است و در پنل ادمین وصل
شده.

مسیر `/dashboard/logs` به داشبورد ریدایرکت می‌شود تا بوکمارک قدیمی
صفحه‌ی سفید نشان ندهد.

## ✅ حل شد — نیازی به کار بک‌اند نیست

| # | مورد | جواب و اقدام فرانت |
|---|---|---|
| ۳ | صدور فاکتور | **خودکار است** → تب «صدور فاکتور» فیگما ساخته نمی‌شود |
| ۵ | اعلان سیستمی | **از سمت سیستم** می‌آید، ادمین نمی‌سازد |
| ۶ | نوع تحویل پلن | در **`plan feature`** مشخص می‌شود؛ در حالت نصب‌توسط‌ما **ادمین خودش** سفارش را به تیکت وصل می‌کند → ✅ همین حالا ساخته شده |
| ۱۳ | ارسال همگانی | محتوای `recipient_ids` مهم نیست، هرچه باشد برای همه می‌رود |
| ۱۴ | شکل `GET /invoice/` | آرایه‌ای از فاکتورها با متادیتا → ✅ کد ما همین را می‌خواند |
| ۱۵ | باطل شدن نشست | **بله** رفرش‌توکن باطل می‌شود → ✅ **درست شد**: کاربر خودکار خارج و به لاگین هدایت می‌شود |
| ۱۶ | منبع نقش‌ها | **`/auth/me`** — گفتید source of truth نباید JWT باشد. ✅ اعمال شد؛ JWT فقط پشتیبان است (توضیح پایین) |
| ۱۷ | promote و توکن قدیمی | **توکن باطل می‌شود** → ✅ کاربر ناچار دوباره وارد می‌شود، پس نقش کهنه نمی‌ماند |
| ۱۸ | قیمت‌گذاری سفارش | **دو مرحله‌ای**: ساخت `plan_price` بعد `quote` با `plan_price_id` → ✅ وصل شد |
| ۱۹ | نام `plan_base_price` | عوض شده به **`plan_price`** → ✅ کد به‌روز شد |
| ۲۰ | مهلت پرداخت | **سمت سیستم** است و داخل پیش‌فاکتور نوشته می‌شود؛ فعلاً در UI لازم نیست |
| ۲۱ | صدور پیش‌فاکتور و فاکتور | **هر دو خودکار**اند → ✅ فرانت اندپوینت ساخت نمی‌زند |

---

## 🟠 هنوز باز

### `roles` در شمای `Profile` نیست *(کوچک ولی لازم)*

گفتید `/auth/me` نقش‌ها را می‌دهد و همان باید منبع اصلی باشد —
موافقم و کد را همین‌طور کردیم.

⚠️ ولی در `openapi (15).json` شمای **`Profile`** فیلد `roles` را
ندارد (فقط `UserSchema` که خروجی لاگین است دارد). یعنی پاسخ واقعی
با اسپک نمی‌خواند.

**درخواست:** `roles` را به شمای `Profile` اضافه کن تا اسپک با
واقعیت یکی شود.

**تا آن‌موقع فرانت چه می‌کند:** اگر پروفایل `roles` داشت همان را
می‌گیرد؛ اگر نداشت از JWT می‌خواند. دلیلش این است که
`/auth/refresh` فقط `access_token` می‌دهد، و بدون این پشتیبان
**ادمین با هر رفرش صفحه (F5) از پنل بیرون می‌افتاد** — یک باگ
واقعی که همین سؤال شما لوش داد.

اگر `roles` در JWT نگذاری هم مشکلی نیست، فقط بگو تا پشتیبان را
برداریم.

### ۷. فیلتر صاحب تیکت *(سؤال قبلی مبهم بود — دوباره توضیح می‌دهم)*

`GET /ticketing/tickets` این فیلتر را دارد:

```
?assigned_to_user_id=5   → کارشناسی که تیکت به او ارجاع شده
```

اما فیلتری برای **صاحب تیکت** (کسی که تیکت را ساخته) ندارد:

```
?user_id=5               → ❌ وجود ندارد
```

**کجا لازم است:** در «پروفایل جامع کاربر» (پنل ادمین) باید تیکت‌های
همان کاربر نشان داده شود. الان آن جدول روی داده‌ی نمونه مانده.

> `GET /admin/orders/` این فیلتر را دارد و جدول سفارش‌های همان صفحه
> با آن وصل شد. فقط تیکت مانده.

### ۱۱. نام به‌جای شناسه — و `public_id`

✅ نکته‌ی `public_id` را اعمال کردیم: لاگ ممیزی حالا
`actor_public_id` را ترجیح می‌دهد.

⚠️ ولی **دو مورد هنوز فقط شناسه‌ی عددی دارند** و `public_id` ندارند:

| اسکیما | الان | نبود |
|---|---|---|
| `OrderOutput` | `user_id` | `user_public_id` |
| `TicketOutSchema` | `user_id`, `assigned_to_user_id` | نسخه‌ی public |
| `AuditLogSchema` | ✅ `actor_public_id` دارد | — |
| `PaymentRecordOutputAdmin` | ✅ `user_public_id` دارد | — |

⚠️ **نکته‌ی مهم‌تر:** درخواست اصلی **نام** بود نه شناسه‌ی امن‌تر.
`public_id` هم UUID است، پس هنوز به کاربر `#0198aa11` نشان می‌دهیم
به‌جای «علی رضایی».

در `PaymentRecordOutputAdmin` فیلد **`user_full_name`** گذاشتید — همان
الگو برای `OrderOutput` و `TicketOutSchema` هم عالی است.

### آپلود مدارک بیشتر برای یک پرداخت *(مورد جدید)*

گفتید ممکن است ادمین از کاربر مدارک بیشتری بخواهد.

⚠️ **الان راهی برای افزودن فایل به رسید موجود نیست.**
`POST /orders/{id}/payments/attachments` شناسه‌ی پرداخت نمی‌گیرد و
بدنه‌اش کاملِ `CreatePaymentRecord` است — یعنی **هر بار یک رکورد
پرداخت جدید می‌سازد**.

دو راه:

1. اندپوینتی مثل نسخه‌ی تیکت که `payment_id` بگیرد و فایل را به رسید
   **موجود** بچسباند:
   ```
   GET /orders/{order_id}/payments/{payment_id}/attachments → { token }
   ```
2. یا همان مسیر تیکت استفاده شود: ادمین در تیکت درخواست می‌دهد و
   کاربر همان‌جا فایل را آپلود می‌کند.

**راه ۲ همین الان کار می‌کند و کد اضافه نمی‌خواهد.** اگر کافی است،
کاری لازم نیست. اگر می‌خواهید مدارک به خود رسید بچسبند، راه ۱ لازم است.

---

## 📌 درباره‌ی تغییرات شکننده

> دفعه‌ی قبل نوشتم `currency` هم حذف شد — درست می‌فرمایید، آن از قبل
> اختیاری و `IRR` بود و مشکلی نداشت.
>
> ولی **`amount` اجباری بود و حذف شد** و همان یکی کد ما را شکست
> (۴۲۲ می‌گرفتیم). حرف اصلی همان است: اگر فیلد **اجباری** حذف یا
> تغییر نام می‌دهد، یک پیام کوتاه قبلش کافی است.

---

## 🙏 حل‌شده در اسپک‌های اخیر

- فاکتور کاربر + دانلود PDF
- آپلود فایل (رسید پرداخت و پیوست تیکت) — زنجیره‌ی توکن کامل شد
- `ticket_id` بالاخره UUID شد
- تغییر رمز با `old_password`
- پروفایل با نام و اطلاعات شرکت
- کاربران ادمین، نقش‌ها، دسترسی‌ها و لاگ ممیزی
- `user_full_name` در پرداخت‌های ادمین
