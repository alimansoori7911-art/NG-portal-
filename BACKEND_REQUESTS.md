# درخواست‌های فرانت از بک‌اند

بر اساس `openapi (14).json` — بازبینی‌شده در ۱۴۰۵/۰۶/۰۸

> این فایل برای فرستادن به تیم بک‌اند است. هر مورد با اسپک ۱۴ چک شده
> و همه واقعاً باز هستند.
>
> **وضعیت فرانت:** هر ۱۳۰ اندپوینت اسپک وصل شده و هر ۶۵ اندپوینت فلو
> پیاده شده. هر چیزی که در ادامه می‌آید یعنی صفحه‌ای که ساخته شده
> ولی داده ندارد.

---

## 🔴 اولویت ۱ — ماژول‌هایی که کلاً وجود ندارند

هر کدام یک صفحه‌ی کامل را بلاک کرده‌اند. UI‌شان ساخته و منتظر است.

### ۱. ماژول لایسنس

تب «مدیریت لایسنس‌ها» در `admin/sales` و جدول لایسنس در پروفایل
جامع کاربر، هر دو روی داده‌ی نمونه‌اند.

```
GET    /admin/licenses                    لیست (کد، کاربر، وضعیت، سرور متصل، انقضا)
POST   /admin/licenses                    ایجاد
GET    /admin/users/{user_id}/licenses    لایسنس‌های یک کاربر
POST   /admin/licenses/{id}/activate      فعال‌سازی
POST   /admin/licenses/{id}/deactivate    غیرفعال‌سازی
POST   /admin/licenses/{id}/renew         تمدید
POST   /admin/licenses/{id}/revoke        فسخ
```

**نکته:** فرم ایجاد دو دکمه دارد — «ایجاد» و «ایجاد به همراه
فعال‌سازی». یا اندپوینت ساخت پارامتر `activate: bool` بگیرد، یا
فرانت دو درخواست پشت‌سرهم بزند. کدام را ترجیح می‌دهید؟

فلو می‌گوید بعد از `TECHNICAL_IN_PROGRESS` برای پلن‌های
`license-only` سرور لایسنس با Celery صدا زده می‌شود — یعنی این ماژول
بخشی از چرخه‌ی اصلی سفارش است، نه یک افزونه.

### ۲. تراکنش‌های مالی

```
GET /admin/transactions   لیست
```
ستون‌های لازم: نام کاربر، شماره پیگیری، کد رهگیری، منبع، وضعیت، تاریخ.

### ۳. صدور فاکتور توسط ادمین

`/invoice/` الان **فقط `GET`** است و **فقط فاکتورهای خود کاربر** را
می‌دهد (نه فیلتر کاربر دارد، نه اندپوینت ادمین).

```
GET  /admin/invoices        لیست همه (نام کاربر، شماره سریال، تاریخ)
POST /admin/invoices        صدور فاکتور
```

⚠️ فلو می‌گوید فاکتور **خودکار** بعد از تأیید پرداخت صادر می‌شود.
اگر واقعاً خودکار است، `POST` لازم نیست — ولی آن‌وقت تب «صدور
فاکتور» در فیگما اضافی است. **کدام درست است؟**

### ۴. ریلیز نوت و اعلان سیستمی

```
GET  /admin/release-notes                لیست
POST /admin/release-notes                ذخیره‌ی پیش‌نویس
POST /admin/release-notes/{id}/publish   انتشار
GET  /admin/system-alerts                اعلان‌های سیستمی
```
اعلان‌ها سه سطح دارند: `info` / `warning` / `error`.

⚠️ مشخص نیست اعلان‌های سیستمی **خودکار از سیستم** می‌آیند یا ادمین
می‌سازد. اگر خودکارند احتمالاً به لاگ سیستم وصل‌اند.

### ۵. لاگ کاربر

```
GET /logs?level=info|error|warning
```
هر رکورد: `level`، `message`، `time`.

---

## 🟠 اولویت ۲ — فیلد یا فیلترِ جامانده

این‌ها ماژول کامل نمی‌خواهند، فقط یک فیلد یا پارامتر.

### ۶. نوع تحویل روی پلن ⚠️ مهم‌ترین

`PlanOutput` فیلدی برای `install-by-us` / `license-only` **ندارد**،
ولی `flow.dot` می‌گوید تحویل از پلن مشخص می‌شود:

```
a_starttech -> i_ticket   [label="install-by-us (from plan)"]
a_starttech -> l_request  [label="license-only (from plan)"]
```

بدون این فیلد، فرانت نمی‌فهمد بعد از خرید باید تیکت نصب نشان دهد یا
کد لایسنس. **یک `delivery_type` روی `PlanOutput` لازم است.**

### ۷. فیلتر صاحب تیکت

`GET /ticketing/tickets` این فیلترها را دارد:
`q, department_id, status, assigned_to_user_id, include`

`assigned_to_user_id` **کارشناس** است نه صاحب تیکت. برای «پروفایل
جامع کاربر» به `user_id` نیاز داریم.

> `GET /admin/orders/` این فیلتر را دارد و جدول سفارش‌های همان صفحه
> با آن وصل شد. فقط تیکت مانده.

### ۸. اتصال فاکتور به سفارش

`PublicInvoiceListItemSchema` نه فیلد `order_id` دارد نه
`GET /invoice/` فیلتر سفارش. پس نمی‌توانیم در صفحه‌ی سفارش لینک
«فاکتور این سفارش» بگذاریم — با اینکه فلو می‌گوید کاربر بعد از
پرداخت فاکتورش را می‌گیرد (`a_invoice -> c_invoice`).

**یک `order_id` روی فاکتور کافی است.**

### ۹. ویرایش پروفایل توسط کاربر

کاربر **هیچ راهی ندارد** اطلاعات خودش را عوض کند:

- `GET /auth/me` فقط خواندنی است
- `PATCH /admin/auth/users/{id}` مال ادمین است
- `POST /auth/contact/verify` یک‌بار پر می‌شود و بعد راهی نیست

اگر نام شرکت یا سمت کاربر عوض شد چه کند؟ **یک `PATCH /auth/me` لازم
است.**

### ۱۰. ویرایش اطلاعات کاربر توسط ادمین

`UserUpdateSchema` فقط `is_active` و `is_blocked` می‌گیرد
(`additionalProperties: false`). دکمه‌ی «ویرایش اطلاعات» در فیگما
هست ولی ادمین نمی‌تواند ایمیل یا شماره را عوض کند.

فعلاً آن دکمه ساخته نشد. قرار است اضافه شود؟

### ۱۱. نام به‌جای شناسه‌ی عددی (بار سوم)

سه جا فقط شناسه‌ی عددی می‌دهند و فیگما نام می‌خواهد:

| جا | الان | لازم |
|---|---|---|
| `OrderOutput` | `user_id` | نام خریدار |
| `TicketOutSchema` | `user_id`, `assigned_to_user_id` | نام کاربر و کارشناس |
| `AuditLogSchema` | `actor_id` | نام ادمین |

فعلاً `#42` نشان می‌دهیم.

> ✅ در `PaymentRecordOutputAdmin` این کار را کردید
> (`user_full_name`) — همان الگو برای این سه هم عالی است.

### ۱۲. برچسب VIP

ستون در فیگما هست ولی در مدل کاربر نیست. فعلاً خط تیره می‌گذاریم
(نه «غیرفعال» که دروغ است). کجا باید ذخیره شود؟

---

## 🟡 اولویت ۳ — ابهام در اسپک

### ۱۳. ارسال همگانی اعلان

در `NotificationAdminCreate` تناقض هست:

```
recipient_ids  →  required، minItems: 1
broadcast      →  "this will overwrite recipient_ids when is True"
```

برای ارسال به همه دقیقاً چه بفرستیم؟ آرایه‌ی خالی؟ یا
`recipient_ids` باید optional شود؟

### ۱۴. شکل پاسخ `GET /invoice/`

`data` در اسپک `anyOf` دارد — هم آرایه‌ی ساده و هم آرایه‌ی تودرتو.
کدام واقعاً برمی‌گردد؟ فعلاً هر دو را تخت می‌کنیم.

### ۱۵. باطل شدن نشست بعد از تغییر رمز

الان فرض گرفتیم **نه** و کاربر را خارج نمی‌کنیم. اگر نشست‌ها باطل
می‌شوند بگویید تا مثل «خروج از همه دستگاه‌ها» رفتار کنیم.

---

## 📌 یک خواهش

تغییر `CreatePaymentRecord` در اسپک ۱۴ **شکننده** بود: `amount` که
اجباری بود و `currency` حذف شدند. کد ما ۴۲۲ می‌گرفت تا وقتی که
پیدایش کردیم.

اگر دفعه‌ی بعد فیلد اجباری حذف یا تغییر نام می‌دهید، از قبل خبر
بدهید — فقط یک پیام کافی است.

---

## 🙏 چیزهایی که در اسپک‌های اخیر حل شد

- فاکتور کاربر + دانلود PDF
- آپلود فایل (رسید پرداخت و پیوست تیکت) — زنجیره‌ی توکن کامل شد
- `ticket_id` بالاخره UUID شد
- تغییر رمز با `old_password`
- پروفایل با نام و اطلاعات شرکت
- کاربران ادمین، نقش‌ها، دسترسی‌ها و لاگ ممیزی
- `user_full_name` در پرداخت‌های ادمین
