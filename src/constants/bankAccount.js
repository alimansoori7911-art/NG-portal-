/**
 * اطلاعات حساب بانکی شرکت — برای واریز آفلاین.
 *
 * چرا اینجاست و نه در بک‌اند؟
 * پرداخت آنلاین در فلو وجود ندارد (تأییدشده از `flow.dot` و
 * `portal.dot`)؛ کاربر بیرون از سایت واریز می‌کند و فقط رسیدش را
 * ثبت می‌کند. پس باید شماره‌ی حساب را ببیند — ولی بک‌اند هیچ
 * اندپوینتی برای این اطلاعات ندارد.
 *
 * ⚠️ راه‌حل موقت. رجوع به «تصمیم ۱» در BACKEND_NEEDS.md
 * وقتی بک‌اند `GET /settings/bank-account` را ساخت، این فایل حذف
 * می‌شود و داده از سرور می‌آید. تا آن موقع هر تغییر حساب یعنی
 * دیپلوی جدید.
 *
 * 🔴 TODO — قبل از انتشار حتماً با مقادیر واقعی شرکت جایگزین شود.
 *    مقادیر فعلی نمونه‌اند و به هیچ حساب واقعی تعلق ندارند.
 */
export const BANK_ACCOUNT = {
    bankName: 'ملت',
    iban: 'IR120120000000000000000000',
    cardNumber: '6104337800000000',
    accountHolder: 'شرکت ان‌جی کوریون',
}

/* شبا در گروه‌های چهارتایی خواناتر است: IR12 0120 0000 ...
   برای کپی کردن از مقدار خام استفاده می‌شود نه این. */
export const formatIban = (iban) => iban.replace(/(.{4})/g, '$1 ').trim()

/* شماره کارت چهار قسمتی با خط تیره */
export const formatCard = (card) => card.replace(/(.{4})(?=.)/g, '$1-')
