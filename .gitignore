# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local permission state (machine-specific)
.claude/settings.local.json

# تنظیمات محلی — آدرس بک‌اند هر کس با دیگری فرق دارد.
# `.env` قبلاً در گیت بود و IP و فلگ‌های یک نفر روی همه اعمال می‌شد.
# الگو در `.env.example` است که در گیت می‌ماند.
.env
.env.local
.env.*.local
