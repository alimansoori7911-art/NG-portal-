# طرح صفحه‌ی پیش‌فاکتور و پرداخت

وضعیت: **پیش‌نویس — منتظر تأیید**
مرجع: `flow.dot` · فیگما ندارد، از صفر طراحی شده
تاریخ: ۱۴۰۵/۰۵/۲۷

---

## چرا این صفحه لازم است

در `flow.dot` این زنجیره هست:

```
a_proforma → c_pay → c_proof → a_verify
صدور پیش‌فاکتور → پذیرش و پرداخت → ثبت رسید → تأیید
```

در UI فعلی **هیچ‌کدام** وجود ندارد. بعد از ثبت سفارش، کد مستقیم
می‌پرد به «تایید تیم فروش» (`setStep(2)` در NewOrderPage).

نتیجه: کاربر نمی‌تواند پرداخت کند. چرخه‌ی سفارش قطع است.

---

## تصمیم کلیدی: این یک صفحه است یا دو تا؟

**پیشنهاد: یک صفحه با دو حالت.**

دلیل: در فلو، «دیدن پیش‌فاکتور» و «پرداخت» دو گام جدا نیستند —
`flow.dot` می‌گوید **«payment = acceptance»** یعنی پرداخت کردن
خودش یعنی قبول کردن پیش‌فاکتور. پس دکمه‌ی جدای «تأیید» غلط است.

```
حالت ۱: پیش‌فاکتور را ببین  →  دکمه‌ی «پرداخت کردم»
حالت ۲: فرم ثبت رسید        →  دکمه‌ی «ثبت رسید»
```

با زدن دکمه‌ی حالت ۱، همان کارت به حالت ۲ تبدیل می‌شود.
بدون تغییر صفحه، بدون رفت‌وبرگشت.

---

## چیدمان — حالت ۱: پیش‌فاکتور

از هندسه‌ی `OrderFormStep` پیروی می‌کند تا با بقیه‌ی مراحل خرید
یکدست باشد:

```
┌──────────────────────────────────────────────────────┐
│  [بج: نام پلن]                                    ✕  │  ← مثل OrderFormStep
│                                                       │
│                   پیش‌فاکتور                          │  ← ۴۰px، --text-secondary
│              شماره: ORD-1404-00123                    │  ← ۱۸px، --text-body
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │  محصول            NG Corion — پلن حرفه‌ای        │ │
│  │  مدت                              یک‌ساله        │ │  ← جدول دوستونه
│  │  تعداد                                    ۱      │ │     خط جداکننده
│  │  ─────────────────────────────────────────────   │ │     --line-default
│  │  مبلغ پایه                   ۱,۲۰۰,۰۰۰ تومان    │ │
│  │  تخفیف                          −۵۰,۰۰۰ تومان    │ │  ← فقط اگر ≠ ۰
│  │  مالیات                         ۱۰۳,۵۰۰ تومان    │ │  ← فقط اگر ≠ ۰
│  │  ═════════════════════════════════════════════   │ │
│  │  قابل پرداخت                 ۱,۲۵۳,۵۰۰ تومان    │ │  ← ۲۴px، --text-bright
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │  اطلاعات حساب جهت واریز                          │ │  ← بلوک جدید
│  │  ─────────────────────────────────────────────   │ │
│  │  بانک             ملت                            │ │
│  │  شماره شبا        IR12 0120 0000 ...      [کپی] │ │  ← dir=ltr
│  │  شماره کارت       6104-3378-••••-••••     [کپی] │ │  ← dir=ltr
│  │  به نام           شرکت ان‌جی کوریون              │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ⓘ پس از واریز، رسید را ثبت کنید تا بررسی شود.       │  ← --text-muted-2
│                                                       │
│      [ پرداخت کردم، ثبت رسید ]   [ بازگشت به لیست ]  │
└──────────────────────────────────────────────────────┘
```

**چرا بلوک حساب اضافه شد:** پرداخت آنلاین وجود ندارد (تأییدشده از
فلو)، پس کاربر بیرون از سایت واریز می‌کند. بدون شماره‌ی حساب، کل
صفحه بی‌فایده است.

منبع داده: `src/constants/bankAccount.js` — ثابت در فرانت تا وقتی
بک‌اند اندپوینت بدهد. رجوع به «تصمیم ۱» در BACKEND_NEEDS.md.

**دکمه‌ی کپی اجباری است:** کسی ۲۶ رقم شبا را دستی تایپ نمی‌کند و
اشتباه تایپ یعنی پول به حساب اشتباه.

**داده‌ها همه از `OrderOutput` می‌آیند** (اندپوینت آماده است):

| نمایش | فیلد بک‌اند |
|---|---|
| شماره | `order_number` |
| محصول | `snapshot_product_name` |
| پلن | `snapshot_plan_name` |
| مدت | `snapshot_plan_base_price_name` |
| تعداد | `requested_quantity` |
| مبلغ پایه | `quoted_amount` |
| تخفیف | `discount_amount` |
| مالیات | `tax_amount` |
| قابل پرداخت | `payable_amount` |
| واحد پول | `snapshot_currency` (همیشه `IRR`) |

> 💱 **واحد پول — تصمیم گرفته شد: تومان**
> بک‌اند همه‌ی مبلغ‌ها را **ریال** می‌دهد و می‌گیرد. تبدیل به تومان
> **فقط در لایه‌ی نمایش** انجام می‌شود (تقسیم بر ۱۰).
> یک تابع واحد `formatToman(rial)` ساخته می‌شود و همه‌جا فقط از آن
> استفاده می‌شود — تقسیم دستی در کامپوننت ممنوع، چون اگر یک‌جا جا
> بیفتد کاربر مبلغ را ۱۰ برابر اشتباه می‌بیند.
> در فرم ثبت رسید هم ورودی کاربر تومان است ولی **ریال** ارسال می‌شود.

---

## چیدمان — حالت ۲: ثبت رسید

```
┌──────────────────────────────────────────────────────┐
│  [بج: نام پلن]                                    ✕  │
│                                                       │
│                    ثبت رسید پرداخت                    │
│           مبلغ قابل پرداخت: ۱,۲۵۳,۵۰۰ تومان          │  ← یادآوری
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │  رسیدهای ثبت‌شده                                 │ │  ← فقط اگر ≥۱ باشد
│  │  ─────────────────────────────────────────────   │ │
│  │  ۵۰۰,۰۰۰ ت · کارت‌به‌کارت · ۱۴۰۵/۰۵/۲۰  ✓ تأیید │ │  ← --status-success
│  │  ۴۰۰,۰۰۰ ت · شبا        · ۱۴۰۵/۰۵/۲۱  ⏳ بررسی │ │  ← --text-muted-2
│  │  ─────────────────────────────────────────────   │ │
│  │  پرداخت‌شده ۹۰۰,۰۰۰ ت  ·  باقی‌مانده ۳۵۳,۵۰۰ ت │ │  ← --text-bright
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│   ┌──────────────────┐  ┌──────────────────┐        │
│   │ مبلغ واریزی *    │  │ روش پرداخت *     │        │  ← دو ستون
│   └──────────────────┘  └──────────────────┘        │     مثل OrderFormStep
│   ┌──────────────────┐  ┌──────────────────┐        │
│   │ نام واریزکننده   │  │ شماره پیگیری *   │        │
│   └──────────────────┘  └──────────────────┘        │
│   ┌──────────────────┐  ┌──────────────────┐        │
│   │ تاریخ واریز      │  │ شماره رسید       │        │
│   └──────────────────┘  └──────────────────┘        │
│   ┌─────────────────────────────────────────┐       │
│   │ توضیحات                                 │       │  ← تمام‌عرض
│   └─────────────────────────────────────────┘       │
│                                                       │
│           [ ثبت رسید ]      [ بازگشت ]               │
└──────────────────────────────────────────────────────┘
```

**چرا فهرست رسیدها اضافه شد — تصمیم ۲:**

بک‌اند از چند رسید پشتیبانی می‌کند:
- `OrderOutput.payments` یک **آرایه** از `PaymentRecordOutput` است
- تأیید هم تک‌تک انجام می‌شود:
  `POST /admin/orders/{order_id}/payments/{payment_id}/verify`

و این با واقعیت می‌خواند: مبلغ‌ها بالاست و ممکن است از سقف روزانه‌ی
کارت‌به‌کارت بیشتر شود، پس کاربر چند بار واریز می‌کند.

**داده‌ی هر ردیف** از `PaymentRecordOutput`: `amount` · `method` ·
`paid_at` · و وضعیت از روی `verified_at` (پر = تأییدشده، تهی = در
انتظار بررسی).

**«باقی‌مانده»** = `payable_amount` منهای مجموع رسیدهای **تأییدشده**.
رسید در انتظار بررسی حساب نمی‌شود چون هنوز قطعی نیست.

**نگاشت فیلدها به `CreatePaymentRecord`** — همه واقعی، هیچ فیلد
اختراعی نیست:

| فیلد فرم | بک‌اند | اجباری؟ |
|---|---|---|
| مبلغ واریزی | `amount` | ✅ بله (>۰) |
| روش پرداخت | `method` | پیش‌فرض `bank_transfer` |
| نام واریزکننده | `payer_name` | اختیاری |
| شماره پیگیری | `tracking_number` | اختیاری در اسپک، **در UI اجباری** |
| تاریخ واریز | `paid_at` | اختیاری |
| شماره رسید | `receipt_ref` | اختیاری |
| توضیحات | `note` | اختیاری |

**روش پرداخت** — گزینه‌ها از enum `PaymentMethod`:
`آفلاین · نقدی · انتقال بانکی · کارت به کارت · سایر`

### چرا شماره پیگیری را اجباری کردم

اسپک اختیاری گرفته، ولی بدون آن ادمین راهی برای تطبیق واریز با
سفارش ندارد. پیشنهاد می‌کنم در UI اجباری باشد.
**این تصمیم نیاز به تأیید دارد** — اگر مخالفی، اختیاری می‌شود.

---

## تصمیم‌های طراحی و دلیلشان

**۱. مبلغ واریزی از پیش پر می‌شود** با `payable_amount` ولی قابل
ویرایش می‌ماند (پرداخت جزئی ممکن است).

**۲. تاریخ واریز شمسی** با `date-fns-jalali` که در پروژه هست، و
هنگام ارسال به میلادی تبدیل می‌شود (بک‌اند گفته UTC).

**۳. اعداد با جداکننده‌ی هزارگان** فارسی نمایش داده می‌شوند.
⚠️ **تابع قالب‌بندی پول در پروژه وجود ندارد** — باید ساخته شود.

**۴. بدون آپلود فایل.** اسپک فقط `receipt_ref` متنی دارد، نه فایل.
اگر آپلود تصویر رسید لازم است، **بک‌اند باید اندپوینت بدهد.**

---

## حالت‌های صفحه

| حالت | نمایش |
|---|---|
| بارگذاری | «در حال دریافت پیش‌فاکتور…» |
| خطا | پیام + دکمه‌ی «تلاش دوباره» |
| سفارش بدون قیمت | «هنوز پیش‌فاکتور صادر نشده است» + بازگشت |
| قبلاً پرداخت شده | «رسید ثبت شده، در انتظار تأیید» |
| موفق | پیام موفقیت + انتقال به لیست سفارش |

---

## مسیر و ورودی

```
/products/buy/orders/:orderId/payment
```

**ورودی‌ها:**
1. دکمه‌ی جدید در ردیف‌های لیست سفارش (وقتی وضعیت
   `QUOTATION_ISSUED` یا `AWAITING_PAYMENT` باشد)
2. اعلان «پیش‌فاکتور صادر شد»

---

## توکن‌های استفاده‌شده

همه از `variables.css`، هیچ رنگ خامی:

```
کارت           --surface-card
حاشیه          --line-strong
جداکننده       --line-default
عنوان          --text-secondary
متن            --text-body
مبلغ نهایی     --text-bright
راهنما         --text-muted-2
دکمه‌ی اصلی     --interactive-border
خطا            --status-error / --status-error-border
```

---

## ✅ تصمیم‌های گرفته‌شده

| سؤال | جواب | تاریخ |
|---|---|---|
| پرداخت آنلاین داریم؟ | **نه.** در فلو نیست؛ فقط ثبت رسید | ۱۴۰۵/۰۵/۲۷ |
| اطلاعات حساب از کجا؟ | فعلاً `constants/bankAccount.js`، بعداً اندپوینت | ۱۴۰۵/۰۵/۲۷ |
| چند رسید مجاز است؟ | **بله** — مطابق بک‌اند (`payments` آرایه است) | ۱۴۰۵/۰۵/۲۷ |
| واحد پول | **تومان** در نمایش، **ریال** در ارسال | ۱۴۰۵/۰۵/۲۷ |
| پرداخت جزئی | **مجاز** — نتیجه‌ی مستقیم تصمیم چند-رسید | ۱۴۰۵/۰۵/۲۷ |

---

## ✅ سؤال‌های باقی‌مانده — همه بسته شدند

| سؤال | جواب | منبع تصمیم |
|---|---|---|
| شماره پیگیری اجباری؟ | **بله** | منطق کاری: بدون آن ادمین نمی‌تواند واریز را در صورت‌حساب بانک پیدا کند |
| آپلود تصویر فیش؟ | **نه** | جستجوی `upload\|file\|image\|عکس` در هر دو دات → صفر نتیجه. اسپک هم فقط `receipt_ref` متنی دارد |
| بعد از ثبت کجا برود؟ | **همان صفحه** | نتیجه‌ی مستقیم تصمیم چند-رسید: کاربر باید باقی‌مانده را ببیند و رسید بعدی را ثبت کند |

### ⚠️ تنها مورد باز — داده، نه تصمیم

**شماره شبا و کارت واقعی شرکت** هنوز داده نشده. در
`src/constants/bankAccount.js` مقدار نمونه با `TODO` گذاشته شده.
**قبل از انتشار حتماً باید عوض شود.**
