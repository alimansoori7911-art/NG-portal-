# نیازهای باز از بک‌اند

آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۰۴ (2026-08-26)

**مرجع اصلی پروژه: [`docs/flow.dot`](docs/flow.dot) و
[`docs/portal.dot`](docs/portal.dot)** — این دو فایل تعریف می‌کنند
پروژه چه کاری انجام می‌دهد. فیگما فقط ظاهر صفحه‌هاست؛ هر جا فیگما و
فلو اختلاف داشتند، **فلو مرجع است** و صفحه‌ی نبوده باید ساخته شود.

مرجع API: `openapi.json` — ERP API v1.0.0

---

# 🚦 وضعیت فعلی — شروع از اینجا

**پوشش فلو: ۹۸.۳٪** (۵۸ از ۵۹ اندپوینت). تنها وصل‌نشده `GET /health`
است که به UI ربطی ندارد.

> **همه‌ی کارهایی که به بک‌اند وابسته نبودند تمام شده‌اند.**
> هر چیزی که الان روی داده‌ی نمونه مانده، واقعاً منتظر اندپوینت است.

## ✅ وصل‌شده و کار می‌کند

| بخش | صفحه |
|---|---|
| احراز هویت · OTP · بازیابی رمز | ورود، ثبت‌نام، فراموشی رمز |
| نشست‌های فعال | `dashboard/sessions` |
| محصولات و پلن‌های عمومی | `products`, `products/buy` |
| سفارش — ثبت، لیست، لغو | `products/buy/*` |
| **پیش‌فاکتور و ثبت رسید** | `products/buy/orders/{id}/payment` |
| **عملیات سفارش ادمین** | `admin/sales` ← **فقط تب اول**؛ تب لایسنس هنوز mock است |
| **کاتالوگ ادمین** | `admin/catalog` |
| تیکتینگ (کاربر و ادمین) | `helpdesk/*`, `admin/support` |
| اعلان‌ها (کاربر و ادمین) | `dashboard/notifications`, `admin/settings` |

## ⏳ منتظر بک‌اند — روی داده‌ی نمونه

| صفحه | اندپوینت لازم |
|---|---|
| کاربران ادمین + پروفایل جامع | `/admin/users` · `/admin/audit-logs` |
| مدیریت مالی | تراکنش‌ها · صدور فاکتور |
| لایسنس‌ها (تب دوم فروش) | کل ماژول لایسنس |
| ریلیز نوت · اعلان سیستمی | ندارند |
| فاکتور کاربر | `GET /invoices` + PDF |
| لاگ کاربر | `GET /logs` |
| مدیریت سرویس | **نامشخص — در فلو هم نیست** |

## 🔴 دو کار فوری که به کد ربطی ندارد

1. **شماره حساب واقعی شرکت** را در
   [`src/constants/bankAccount.js`](src/constants/bankAccount.js)
   بگذار. مقدار فعلی نمونه است و با `TODO` علامت خورده —
   **اگر عوض نشود پول کاربر به حساب اشتباه می‌رود.**
2. **پنج سؤال بک‌اند** که ته همین فایل آماده است؛ مهم‌ترینش امنیتی
   است (`/auth/password/change` هنوز رمز فعلی نمی‌گیرد).

## 📄 سندهای دیگر

- [`DESIGN_PAYMENT.md`](DESIGN_PAYMENT.md) — طرح صفحه‌ی پرداخت و
  تصمیم‌هایی که پشتش بود
- [`BACKEND_REVIEW.md`](BACKEND_REVIEW.md) — بازبینی امنیتی کد بک‌اند

---

# 📌 جمع‌بندی نهایی — تطبیق فیگما با فلو

## اندازه‌گیری دقیق

دو فایل دات با اسکریپت تجزیه شدند و تک‌تک اندپوینت‌هایشان با
`src/services/*.js` مقابله شد.

**حجم دات‌ها**

| فایل | گره‌ی واقعی | گره‌ی توضیحی (enum/legend/note) | یال |
|---|---|---|---|
| `flow.dot` | ۲۳ | ۵ | ۳۳ |
| `portal.dot` | ۵۷ | ۴۶ | ۱۳۰ |
| **جمع** | **۸۰** | ۵۱ | ۱۶۳ |

**پوشش اندپوینت — عدد دقیق**

```
اندپوینت یکتا در دات‌ها : ۵۹
وصل‌شده در کد ما        : ۵۳
وصل‌نشده                : ۶
پوشش                    : ۸۹.۸٪
```

> اصلاح تخمین قبلی: عدد «۷۰٪» که شفاهی گفتم تخمین چشمی بود.
> اندازه‌گیری واقعی **۸۹.۸٪** است. یعنی وضعیت اتصال API بهتر از
> چیزی است که فکر می‌کردم.

**۶ اندپوینتی که وصل نشده‌اند** — هر ۶ تا در `src/` **اصلاً وجود ندارند**:

| اندپوینت | چرا مهم است |
|---|---|
| `GET /admin/orders/` | ورودی کل عملیات سفارش ادمین |
| `POST /admin/orders/{id}/quote` | صدور پیش‌فاکتور — گام کلیدی فلو |
| `POST /admin/orders/{id}/payments/{pid}/verify` | تأیید رسید پرداخت |
| `POST /admin/orders/{id}/status` | تغییر وضعیت، رد کردن، تکمیل |
| `POST /admin/orders/{id}/tickets/link` | اتصال سفارش به تیکت نصب |
| `GET /health` | فقط سلامت سرور — به UI ربطی ندارد |

**یعنی: ۵ اندپوینت از ۶ تا، همگی «عملیات سفارش در پنل ادمین»اند.**
این تنها حلقه‌ی کاملاً قطع فلو است — از «بررسی سفارش» تا «تکمیل».

## تفکیک: چقدر از فلو صفحه دارد؟

پوشش API با پوشش UI فرق دارد. از ۸۰ گره‌ی واقعی:

| وضعیت | تعداد | درصد |
|---|---|---|
| هم اندپوینت، هم UI | ~۵۶ | ۷۰٪ |
| اندپوینت دارد، UI ندارد | ~۱۷ | ۲۱٪ |
| نه اندپوینت، نه UI | ~۷ | ۹٪ |

**پس «۷۰٪» عدد درستی بود — ولی برای پوشش UI، نه پوشش API.**
هر دو عدد را جدا نگه می‌داریم:

- **پوشش API: ۸۹.۸٪** (۵۳ از ۵۹)
- **پوشش UI: ~۷۰٪**

فاصله‌ی این دو یعنی: بخش زیادی از کار باقی‌مانده **فقط ساخت UI**
است، نه انتظار برای بک‌اند.

## گام‌های فلو که هیچ صفحه‌ای ندارند

| گام در فلو | فیگما | اندپوینت | نتیجه |
|---|---|---|---|
| پذیرش پیش‌فاکتور و پرداخت | ❌ | ❌ | باید از صفر طراحی شود |
| ثبت رسید پرداخت | ❌ | ✅ `POST /orders/{id}/payments` | فقط UI لازم است |
| لغو سفارش | ❌ | ✅ `POST /orders/{id}/cancel` | فقط دکمه لازم است |
| مشاهده‌ی پیش‌فاکتور | ❌ | ❌ | هر دو لازم است |
| قیمت اختصاصی کاربر (ادمین) | ❌ | ✅ `POST /admin/orders/{id}/quote` | فقط UI لازم است |
| تأیید رسید (ادمین) | ❌ | ✅ `.../payments/{pid}/verify` | فقط UI لازم است |
| پیگیری نصب با تیکت | ❌ | ✅ (تیکتینگ) | ارتباط سفارش↔تیکت در UI نیست |

## ⚠️ تناقض: «مدیریت سرویس»

صفحه‌ی اول داشبورد کاربر **در هیچ‌کدام از دو دات وجود ندارد**.
یا فلو ناقص است یا این صفحه اضافه است — **باید از کارفرما پرسیده شود.**

## قواعدی که دات‌ها روشن کردند و در UI فعلی نیست

- **دمو = آزمایشی، فقط یک‌بار برای هر کاربر.** درخواست دوم خودکار رد
  می‌شود. UI فعلی این را به کاربر نمی‌گوید.
- **پرداخت یعنی پذیرش پیش‌فاکتور** — دکمه‌ی جداگانه‌ی «تأیید» لازم نیست.
- **فاکتور خودکار بعد از تأیید پرداخت** صادر و ایمیل می‌شود، نه دستی.
- **تایم‌اوت پرداخت** (مثلاً یک هفته) → رد خودکار سفارش.
- **لغو فقط کاربر، رد فقط ادمین** — هر دو در هر مرحله‌ی غیرنهایی.
- **`plan_base_price`** یعنی قیمت اختصاصی که ادمین برای همان کاربر
  می‌سازد و به سفارش می‌چسباند.
- **نوع تحویل از پلن می‌آید:** `install-by-us` (تیکت خودکار ساخته
  می‌شود) یا `license-only` (سرور لایسنس با Celery صدا زده می‌شود).

---

# 📌 خلاصه‌ی آنچه از بک‌اند کم داریم

به ترتیب اهمیت:

| # | مورد | وضعیت |
|---|---|---|
| ۱ | **فاکتور و پیش‌فاکتور** | در فلو هست، در اسپک **نیست**. خود `flow.dot` می‌نویسد «not exposed in openapi» |
| ۲ | **نوع تحویل پلن** | `PlanOutput` فیلد `install-by-us`/`license-only` ندارد |
| ۳ | **تغییر رمز بدون رمز فعلی** | 🔴 امنیتی — هنوز `current_password` نمی‌گیرد |
| ۴ | **لایسنس‌ها** | هیچ اندپوینتی نیست |
| ۵ | **کاربران ادمین** | لیست، نقش، وضعیت، لاگ ممیزی — هیچ‌کدام |
| ۶ | **لاگ‌های کاربر** | `GET /logs` وجود ندارد |
| ۷ | **`ticket_id` متناقض** | در `LinkOrderTicket` عدد، بقیه‌جا UUID (سومین بار) |
| ۸ | **ارسال همگانی اعلان** | `recipient_ids` اجباری ولی با `broadcast` نادیده گرفته می‌شود |
| ۹ | **ریلیز نوت‌ها** | UI ساخته شده، اندپوینت ندارد |
| ۱۰ | **سرویس‌های سازمان** | صفحه‌ی «مدیریت سرویس» اندپوینت ندارد |
| ۱۱ | **اطلاعات حساب بانکی** | چون پرداخت آفلاین است، کاربر باید شبا/کارت را ببیند. فعلاً ثابت در فرانت — [تصمیم ۱](#-تصمیم-۱--اطلاعات-حساب-بانکی) |

---

# 📌 غلط‌های کد فعلی ما (نیاز به بک‌اند ندارد)

1. ~~**دو وضعیت سفارش جا افتاده**~~ ✅ درست شد — `FAILED` و `REFUNDED`
   به [orderService.js](src/services/orderService.js) اضافه شدند.
2. **یک‌بارمصرف بودن دمو** هنوز در UI نمایش داده نمی‌شود. ⏳
   طبق فلو هر کاربر فقط **یک بار** می‌تواند دمو بگیرد و درخواست دوم
   خودکار رد می‌شود، ولی دکمه‌ی «درخواست دمو» این را نمی‌گوید.
   کاربر بعد از رد شدن تازه می‌فهمد.

---

# 📌 ترتیب کار — تاریخچه و باقی‌مانده

~~**فاز ۱ — عملیات سفارش ادمین**~~ ✅ انجام شد (`677ce27`)
پنج اندپوینت `/admin/orders/*` وصل شدند؛ حلقه‌ی قطع فلو بسته شد.

~~**فاز ۲ — سمت کاربرِ همان چرخه**~~ ✅ انجام شد (`081c957`, `fb1f008`)
پیش‌فاکتور، ثبت رسید، لغو سفارش، و دو وضعیت جامانده.

~~**فاز ۳ — اتصال صفحه‌های آماده**~~ ✅ انجام شد (`8910cde`, `fb1f008`)
کاتالوگ ادمین و نشست‌های فعال.

**فاز ۴ — نیاز به تصمیم کارفرما** ← اینجاییم

- تکلیف صفحه‌ی «مدیریت سرویس» (در فلو نیست)
- شماره حساب واقعی برای `bankAccount.js`
- جواب پنج سؤال بک‌اند (ته همین فایل)

**فاز ۵ — منتظر بک‌اند**

فاکتور · لایسنس · کاربران ادمین · لاگ‌ها · ریلیز نوت

### ⚠️ نکته درباره‌ی درصدها در بخش بعدی

بخش «اندازه‌گیری دقیق» پایین‌تر عدد **۸۹.۸٪** را نشان می‌دهد. آن
عدد **مربوط به قبل از فازهای ۱ تا ۳ است** و به‌عنوان تاریخچه نگه
داشته شده. **عدد فعلی ۹۸.۳٪ است.**

---

## 🔴 بلاکر — تا حل نشوند صفحه ساخته نمی‌شود

### ۱. تغییر رمز بدون رمز فعلی (امنیتی)

`ChangedInput` سه فیلد دارد: `username`, `email`, `password` — همه اختیاری و
**بدون `old_password`**.

اگر رمز فعلی چک نشود، هر کسی که به یک نشست باز دسترسی پیدا کند
(مثلاً لپ‌تاپ قفل‌نشده) می‌تواند رمز را عوض کند و حساب را تصاحب کند.

**وضعیت:** بک‌اند گفته «هنوز نزدم» — در فاز profile می‌آید.
**اثر:** فرم تغییر رمز در صفحه‌ی حساب کاربری قابل ساخت نیست.

### ۲. اطلاعات پروفایل در `UserSchema`

الان فقط `created_at`, `identifiers`, `roles` برمی‌گردد.
نام، نام خانوادگی و نام شرکت لازم است.

**وضعیت:** بک‌اند تأیید کرده اضافه می‌کند — فاز profile.
**اثر:** `getDisplayName` در [authStore.js](src/store/authStore.js) فقط نام
کاربری را نشان می‌دهد. وقتی اضافه شد باید بازنویسی شود.

---

## 🟡 ابهام — کد فعلی کار می‌کند ولی دفاعی نوشته شده

### ۳. ساختار `value_json`

بک‌اند توضیح داده که «ویژگی‌های متغیر محصول» است، ولی **شکل داده** هنوز
تایپ نشده: مقدار خام (`5`) یا شیء (`{"value": 5}`)؟

**وضعیت:** [planMapper.js](src/modules/products/utils/planMapper.js) هر سه حالت
رایج را پوشش می‌دهد. وقتی داده‌ی واقعی دیده شد، به یک خط ساده تبدیل شود.

### ۴. توکن بازیابی رمز

`POST /auth/otp/reset-password/verify` پاسخش `ApiResponse` خام است (بدون تایپ).
بک‌اند شفاهی گفته `claim_token` برمی‌گردد.

**وضعیت:** [ForgotPasswordPage.jsx](src/modules/auth/pages/ForgotPasswordPage.jsx)
اول `claim_token` و بعد `reset_token` را چک می‌کند و اگر هیچ‌کدام نبود
کاربر را به مرحله‌ی کد برمی‌گرداند.
**درخواست:** تایپ شدن پاسخ در اسپک.

---

## 🟢 قول داده شده — منتظر اسپک جدید

### ۵. نشست فعلی (`is_current`) — انجام شده، اسپک نرسیده

بک‌اند تأیید کرده که به `RefreshToken` اضافه شده:

```
+ is_current: bool
+ session_id: UUID7        ← با id (همان refresh_token.id / jti) فرق دارد
  DELETE /auth/sessions/{id}  →  path از int به UUID7 تغییر کرد
```

**⚠️ نکته‌ی مهم:** `session_id` و `id` دو چیز متفاوت‌اند. برای نمایش و حذف
باید `session_id` استفاده شود.

**وضعیت:** فرانت طبق همین فرض جلو می‌رود؛ با رسیدن اسپک تأیید شود.

---

## 🟡 عملیات سفارش ادمین — وصل شد، یک ابهام دارد

پنج اندپوینت `/admin/orders/*` وصل شدند و «حلقه‌ی گم‌شده»ی فلو بسته شد:
حالا ادمین می‌تواند سفارش را ببیند، قیمت بزند، رسید را تأیید کند و
وضعیت را عوض کند.

### ❓ ابهام: `plan_base_price` در صدور پیش‌فاکتور

`flow.dot` (خط ۳۵) می‌گوید:

```
Create plan_base_price (per-user pricing) & attach by id
```

یعنی ادمین باید **اول یک قیمت اختصاصی بسازد** و بعد با شناسه به سفارش
بچسباند. ولی `AdminPriceOrder` (بدنه‌ی `quote`) چنین فیلدی ندارد:

```
quoted_amount · discount_amount · tax_amount · admin_note
```

**تصمیم فعلی:** تفسیر ساده‌تر پیاده شد — ادمین مستقیم مبلغ می‌زند،
چون این با اسپک می‌خواند. اگر واقعاً باید `plan_base_price` ساخته
شود، فرم باید دو مرحله‌ای شود.

**سؤال از بک‌اند:** کدام درست است؟

### ℹ️ ستون «خریدار» شناسه نشان می‌دهد

`OrderOutput` فقط `user_id` عددی دارد، نه نام کاربر. تا اضافه شدن نام
از سمت بک‌اند، `#42` نمایش داده می‌شود. (همان مشکل جدول تیکت ادمین)

---

## 🔵 ماژول سفارش — وصل شد، ولی فیلد کم دارد

`/orders/*` (۶ اندپوینت) به UI وصل شد. یک شکاف باقی است:

### فرم ثبت سفارش هفت فیلد دارد که `CreateOrder` نمی‌پذیرد

فرم فیگما این‌ها را می‌گیرد:
`نام کاربری، نام سازمان، شماره تماس، ایمیل، شناسه سازمان، MK ID، آدرس`

اما `CreateOrder` فقط این‌ها را دارد:
`plan_id، product_id، plan_base_price_id، quantity، customer_note`

**راه‌حل موقت:** هر هفت فیلد به‌صورت متن ساخت‌یافته در `customer_note`
ارسال می‌شوند (تابع `buildOrderNote` در NewOrderPage).

**راه‌حل درست:** یا بک‌اند این فیلدها را به `CreateOrder` اضافه کند،
یا (بهتر) از پروفایل کاربر بخواند و فرم فقط نمایش دهد.

⚠️ **`MK ID` در هیچ اسکیمای بک‌اندی وجود ندارد و معنی‌اش هم مشخص نیست.**

### دمو تفکیک آنلاین/آفلاین ندارد

فیگما دو دکمه داشت («دمو آنلاین» و «دمو آفلاین») ولی `POST /orders/demo`
فیلدی برای تفکیک ندارد. فعلاً یک دکمه گذاشته شد.
اگر تفکیک لازم است، فیلد `demo_type` اضافه شود.

### لغو سفارش UI ندارد

`POST /orders/{id}/cancel` آماده است ولی هیچ دکمه‌ای در فیگما برای لغو
سفارش وجود ندارد. (`CloseOrderModal` مربوط به بستن فرم است، نه لغو سفارش.)

### ثبت رسید پرداخت UI ندارد

`POST /orders/{id}/payments` آماده است ولی فیگمای صفحه‌ی پرداخت نرسیده.

---

## 🟣 محصولات و کاتالوگ — بک‌اند کامل است، سه تداخل حل شد

این تنها بخش ادمین است که بک‌اندش کامل آماده است. سه ناسازگاری با
فیگما **بدون تغییر بک‌اند** حل شد:

| تداخل | راه‌حل |
|---|---|
| «مدت اعتبار» روی پلن نیست | فیلد از input به select تبدیل شد؛ گزینه‌ها از `GET /admin/billing-term/` |
| `code` اجباری ولی در فیگما نیست | خودکار از `external_plan_code` ساخته می‌شود ([planCode.js](src/modules/admin/utils/planCode.js)) |
| `product_id` در مسیر است نه بدنه | فعلاً اولین محصول؛ با آمدن محصول دوم به dropdown نیاز است |

### تنها سؤال باز

**آیا بک‌اند می‌تواند `code` را خودش بسازد؟**
اگر بله، `buildPlanCode` حذف می‌شود و ریسک تکراری‌شدن `code` هم از بین
می‌رود (الان اگر دو پلن `external_plan_code` مشابه داشته باشند،
`code` یکسان تولید می‌شود و بک‌اند احتمالاً ۴۰۹ می‌دهد).

### نکته برای هنگام اتصال

ساخت پلن دو مرحله است، چون مدت اعتبار روی قیمت است نه پلن:
1. `POST /admin/products/{id}/plans` → پلن ساخته می‌شود
2. `POST /admin/plans/{id}/prices` با `term_code` انتخاب‌شده

---

## 🔴 فروش و مشتریان — UI ساخته شد، بک‌اند تقریباً هیچ ندارد

### تب ۱: پیگیری سفارش‌ها

`GET /admin/orders/` وجود دارد ولی ستون‌های فیگما با `OrderOutput`
کامل نمی‌خوانند:

| ستون فیگما | در بک‌اند |
|---|---|
| کد سفارش | ✅ `order_number` |
| خریدار | ⚠️ فقط `user_id` — نام کاربری لازم است |
| مبلغ | ✅ `payable_amount` |
| وضعیت پرداخت | ⚠️ `status` هست ولی وضعیت *پرداخت* جدا نیست |
| پلن | ✅ `snapshot_plan_name` |
| تاریخ | ✅ `created_at` |

**دو دکمه‌ی نوار عملیات اندپوینت ندارند:**
- «صدور فاکتور» — نزدیک‌ترین چیز `POST /admin/orders/{id}/quote` است
  ولی آن پیش‌فاکتور است نه فاکتور
- «مشاهده فاکتور» — هیچ اندپوینتی برای دریافت/دانلود فاکتور نیست

### تب ۲: مدیریت لایسنس‌ها — هیچ اندپوینتی وجود ندارد

**کل این تب بدون بک‌اند است.** موارد لازم:

```
GET    /admin/licenses               لیست (کد لایسنس، کاربر، وضعیت، سرور متصل)
POST   /admin/licenses               ایجاد (کد لایسنس، نام کاربری، آدرس سرور)
POST   /admin/licenses/{id}/activate     فعال‌سازی
POST   /admin/licenses/{id}/deactivate   غیرفعال‌سازی
POST   /admin/licenses/{id}/renew        تمدید زمان
POST   /admin/licenses/{id}/revoke       فسخ
```

**نکته‌ی مهم:** فرم ایجاد دو دکمه دارد — «ایجاد لایسنس» و «ایجاد به
همراه فعال‌سازی». یعنی یا اندپوینت ساخت باید پارامتر `activate: bool`
بگیرد، یا فرانت باید دو درخواست پشت‌سرهم بزند.

همچنین فیگما حالت خطای «این نام کاربری در سیستم موجود نمی باشد» را
نشان می‌دهد، پس اندپوینت ساخت باید ۴۲۲ با نام فیلد برگرداند.

---

## 🔴 مدیریت مالی — UI ساخته شد، بک‌اند هیچ ندارد

هر دو تب کاملاً بدون بک‌اند هستند.

⚠️ توجه: `/admin/billing-term/` (۵ اندپوینت) که در اسپک هست، **ربطی
به این بخش ندارد** — آن «مدت اعتبار پلن» است (ماهانه/سالانه)، نه مالی.

### تب ۱: تراکنش‌ها

```
GET /admin/transactions    لیست تراکنش‌ها
```
ستون‌های لازم: نام کاربر، شماره پیگیری، کد رهگیری، منبع (مثل
«بانک ملت PSP»)، وضعیت، تاریخ.

### تب ۲: مدیریت و صدور فاکتور

```
GET  /admin/invoices              لیست (نام کاربر، شماره سریال، تاریخ صدور)
POST /admin/invoices              صدور فاکتور الکترونیکی
GET  /admin/invoices/{id}         مشاهده فاکتور (الکترونیکی/کاغذی)
```

**فرم صدور چهار فیلد دارد:** شماره سریال فاکتور، نوع پلن،
شناسه پرداخت، شماره MK.

⚠️ **«شماره MK» دوباره ظاهر شد** — همان فیلد مبهمی که در فرم سفارش
هم بود و هنوز معلوم نیست چیست. لطفاً تعریفش را بگیر.

---

## 🔴 ابزارها و تنظیمات سیستم — UI ساخته شد، بک‌اند هیچ ندارد

هر سه تب بدون بک‌اند هستند.

### تب ۱: ارسال نوتیفیکیشن / پیام

```
GET  /admin/notifications     لیست (عنوان، کانال، تاریخ انتشار)
POST /admin/notifications     ارسال — { title, channel, body }
```
کانال‌ها: `SMS` | `E-mail` | `In App Notification`

### تب ۲: اعلان‌های سیستمی

```
GET /admin/system-alerts      لیست اعلان‌ها
```
هر اعلان: `type` (info/error/warning)، `title`، `message`، `date`، `time`.
رنگ‌ها در فیگما: info آبی، error قرمز، warning نارنجی.

⚠️ مشخص نیست این اعلان‌ها **خودکار از سیستم** می‌آیند یا ادمین می‌سازد.
اگر خودکارند، احتمالاً به لاگ سیستم وصل‌اند.

### تب ۳: ریلیز نوت‌ها

```
GET  /admin/release-notes            لیست (عنوان، وضعیت، تاریخ انتشار)
POST /admin/release-notes            ذخیره (پیش‌نویس)
POST /admin/release-notes/{id}/publish   انتشار
POST /admin/release-notes/{id}/image     بارگذاری عکس
```
وضعیت‌ها: «منتشر شده» و «عدم انتشار» — یعنی مفهوم پیش‌نویس/انتشار دارد.
فرم سه دکمه دارد: بارگذاری عکس، انتشار، ذخیره.

---

## 🔴 داشبورد کاربر — دو صفحه بک‌اند ندارند

### ~~اعلان‌های کاربر~~ ✅ وصل شد

ماژول `notifications` در اسپک آمد و صفحه وصل شد.
دو مورد باز باقی مانده — بخش «🟡 اعلان‌ها» پایین‌تر را ببینید.

### فاکتورهای کاربر

```
GET /invoices                لیست (تاریخ، پلن، شماره سریال)
GET /invoices/{id}/pdf       دریافت فاکتور به‌صورت PDF
```
⚠️ فیگما صریح می‌گوید «برای مشاهده فاکتور به‌صورت pdf روی ردیف کلیک
نمایید» — پس اندپوینت PDF لازم است.

### لاگ‌های کاربر

```
GET /logs?level=info|error|warning
```
هر لاگ: `level`، `message`، `time`.
سه سطح با رنگ‌های فیگما: info آبی، error قرمز، warning نارنجی.

⚠️ **«وضعیت پشتیبانی» از منو حذف شد** — در فیگما وجود نداشت.
**«حسابداری» به «فاکتور» تغییر نام داد** — طبق فیگما.

---

## 🟠 پنل ادمین — UI ساخته شده، بک‌اند ندارد

`/admin/*` در اسپک فقط **کاتالوگ محصولات** است (محصول، پلن، قیمت، قابلیت،
دسته‌بندی، نسخه، دوره‌ی صورت‌حساب) به‌علاوه‌ی سفارش‌ها.

صفحات زیر ساخته شده‌اند ولی هیچ اندپوینتی ندارند:

| صفحه | نیاز |
|---|---|
| لیست کاربران | `GET /admin/users` با صفحه‌بندی — فیلدها: ایمیل، نام کاربری، شماره تماس، وضعیت، نقش، برچسب VIP، تاریخ ثبت‌نام |
| لاگ ممیزی | `GET /admin/audit-logs` — فیلدها: نوع عملیات، ادمین مربوطه، IP، زمان |
| تغییر نقش | `PATCH /admin/users/{id}/role` |
| تغییر وضعیت | `PATCH /admin/users/{id}/status` (فعال/غیرفعال) |
| برچسب VIP | جای این فیلد در مدل کاربر مشخص نیست |
| حذف کاربر | `DELETE /admin/users/{id}` |
| پروفایل جامع | اطلاعات شخصی + تاریخچه خرید + لایسنس‌ها + تیکت‌ها |
| لایسنس‌های کاربر | `GET /admin/users/{id}/licenses` — کد لایسنس، وضعیت، تاریخ فعال‌سازی/انقضا، سرور متصل |

**نکته‌ی مثبت:** `GET /admin/orders/` وجود دارد و می‌تواند بخش «تاریخچه
خرید و سفارشات» پروفایل جامع را تأمین کند.

---

## 🟡 تیکتینگ — وصل شد، ولی سه مورد باز است

ماژول `ticketing` آمد و همه‌ی صفحات تیکت به آن وصل شدند
(لیست، جزئیات، چت، ثبت تیکت، پنل ادمین).

### ۱. نام کاربری و کارشناس در جدول ادمین

`TicketOutSchema` فقط `user_id` و `assigned_to_user_id` عددی می‌دهد،
نه نام. جدول فیگما دو ستون «نام کاربری» و «کارشناس تخصیص یافته» دارد.

**فعلاً:** شناسه با `#` نشان داده می‌شود (`#42`).

**نیاز:** یا نام در خروجی تیکت بیاید، یا `include=user` اضافه شود.

### ۲. تناقض نوع `ticket_id`

```
POST /admin/orders/{id}/tickets/link  →  ticket_id: integer
GET  /ticketing/tickets/{ticket_id}   →  ticket_id: UUID
```

`OrderTicketLinkOutput.ticket_id` هم UUID است. یعنی `LinkOrderTicket`
جا مانده. تا اصلاح نشود، وصل کردن تیکت به سفارش شکست می‌خورد.

### ۳. تیکت فیلد «توضیحات» ندارد

`TicketCreateSchema` فقط `department_id` و `subject` می‌گیرد، ولی فرم
فیگما یک textarea «توضیحات» دارد و فیلد «موضوع» ندارد.

**راه‌حل فعلی:** تیکت با موضوعِ ساخته‌شده از خط اول توضیحات ثبت می‌شود
و کل متن به‌عنوان اولین پیام با `/reply` می‌رود. یعنی هر ثبت تیکت
دو درخواست است.

**پیشنهاد:** `TicketCreateSchema` یک فیلد اختیاری `body` بگیرد و
خودش اولین پیام را بسازد.

---

## 🟡 اعلان‌ها — وصل شد، دو مورد باز است

ماژول `notifications` آمد و وصل شد: صفحه‌ی اعلان‌های کاربر، زنگ
پنل ادمین، و تب «ارسال نوتیفیکیشن». تازه‌سازی خودکار هر ۳۰ ثانیه
(طبق گفته‌ی خودتان) و فقط وقتی تب مرورگر باز است.

### ۱. ارسال همگانی: `recipient_ids` چه باید باشد؟

در `NotificationAdminCreate` تناقض هست:

```
recipient_ids  →  required، minItems: 1
broadcast      →  "this will overwrite recipient_ids when is True"
```

یعنی برای ارسال به همه، `broadcast: true` می‌فرستیم ولی اسپک
هنوز حداقل یک شناسه می‌خواهد. **الان چه چیزی باید بفرستیم؟**
آرایه‌ی خالی؟ یا `recipient_ids` باید optional شود؟

فعلاً فرانت هرچه ادمین وارد کرده می‌فرستد؛ اگر خالی باشد و
broadcast روشن باشد، ممکن است ۴۲۲ بگیرد.

### ۲. اعلان بنر تصویر ندارد

فیگما برای بعضی اعلان‌ها (مثل ریلیز نوت) بنر تصویر دارد، ولی
`NotificationResponse` فیلد تصویر ندارد.

**فعلاً:** کارت‌ها بدون تصویر رندر می‌شوند.

**نیاز (اگر تصویر لازم است):** یک فیلد `image_url` اختیاری.

### نکته‌ی کوچک

کانال‌ها در فیگما سه‌تا بودند (`SMS` / `E-mail` / `In App`) ولی
بک‌اند فقط `in_app` و `sms` دارد. ایمیل حذف شد.

---

## ⚪ ماژول‌هایی که هنوز وجود ندارند

### ۷. سرویس‌های داشبورد سازمانی

`ServicesPage` روی `mockServices.js` است و اندپوینتی ندارد.

---

## 📋 موارد حل‌شده (برای تاریخچه)

| موضوع | نتیجه |
|---|---|
| نقش ادمین | رشته‌ی `"admin"` در آرایه‌ی `roles` |
| امنیت `/admin/*` | همه‌ی اندپوینت‌ها `security` گرفتند |
| صفحه‌بندی | `?page=&limit=` — پاسخ: `limit`, `total`, `has_previous` (نه `per_page`/`total_items`/`has_prev`) |
| `task_id` | ابزار debug داخلی Celery — به فرانت مربوط نیست، پیاده نشد |
| `OTPRequestOutput.otp` | فقط در حالت debug پر می‌شود — نباید به آن تکیه کرد |
| `company_id` | رشته (شناسه‌ی ملی شرکت)، اختیاری |
| `/auth/contact/verify` | نیاز به لاگین دارد؛ ۲۰۲ یعنی «شماره تأیید نشده» |
| کدهای خطا | CAPITAL_CASE؛ پیام‌ها فعلاً انگلیسی — ترجمه در [constants/auth.js](src/constants/auth.js) |
| `/products/{slug}/plans` | در اسپک جدید **تخت** شد (قبلاً تودرتو بود) |

---

## ⚖️ تصمیم‌های گرفته‌شده — صفحه‌ی پرداخت

تاریخ: ۱۴۰۵/۰۵/۲۷ · تصمیم‌گیرنده: کارفرما (علی)

### ✅ تأیید شد: پرداخت آنلاین وجود ندارد

در **هیچ‌کدام** از دو فایل دات، درگاه پرداخت نیست. جستجوی
`gateway|online|درگاه` صفر نتیجه داد. شواهد:

- روش‌های پرداخت (`portal.dot:148`):
  `offline / cash / bank_transfer / card_to_card / other` — هیچ‌کدام آنلاین نیست
- بعد از ثبت پرداخت (`portal.dot:311`): `-> order_payment_verify [label="admin review"]`
  یعنی **انسان** تأیید می‌کند، نه درگاه
- نام همه‌ی گره‌ها `Payment **Proof**` است — یعنی رسیدِ پرداختی که
  بیرون از سیستم انجام شده

**نتیجه برای UI:** هیچ دکمه‌ی «پرداخت آنلاین» یا هدایت به درگاه
ساخته نمی‌شود. کاربر بیرون واریز می‌کند و فقط رسیدش را ثبت می‌کند.

### 📌 تصمیم ۱ — اطلاعات حساب بانکی

**مشکل:** اگر پرداخت بیرونی است، کاربر باید بداند به کجا واریز کند.
این اطلاعات **نه در فلو هست، نه در بک‌اند**.

**تصمیم:** فعلاً به‌صورت ثابت در فرانت، ولی در فایل مستقل
`src/constants/bankAccount.js` تا وقتی بک‌اند اندپوینت داد فقط همان
یک فایل عوض شود.

> ⚠️ **یادداشت برای آینده — فراموش نشود:**
> این یک راه‌حل موقت است. شماره‌ی حساب در کد فرانت یعنی هر تغییر
> حساب نیاز به دیپلوی جدید دارد. **از بک‌اند اندپوینت بخواهیم**، مثلاً:
> ```
> GET /settings/bank-account
> → { bank_name, iban, card_number, account_holder }
> ```
> تا آن موقع، هر بار که حساب عوض شد باید یادمان باشد فایل بالا
> به‌روز شود.

**نکته‌ی UI:** کنار شبا و شماره‌ی کارت **دکمه‌ی کپی** لازم است؛
تایپ دستی ۲۶ رقم یعنی احتمال اشتباه و گم شدن پول.

### 📌 تصمیم ۲ — چند رسید برای یک سفارش

**مطابق بک‌اند عمل می‌کنیم.** اسپک این را پشتیبانی می‌کند:

- `OrderOutput.payments` یک **آرایه** از `PaymentRecordOutput` است
- `verify` هم `payment_id` جدا می‌گیرد:
  `POST /admin/orders/{order_id}/payments/{payment_id}/verify`

پس **چند رسید مجاز است** — که با واقعیت هم می‌خواند: مبلغ‌ها بالاست
و ممکن است از سقف روزانه‌ی کارت‌به‌کارت بیشتر شود.

**نتیجه برای UI:** صفحه‌ی پرداخت باید فهرست رسیدهای ثبت‌شده را با
وضعیت هرکدام نشان دهد، نه فقط یک فرم تکی. مجموع تأییدشده‌ها در برابر
مبلغ کل هم نمایش داده شود.

### 📌 تصمیم ۳ — واحد پول: تومان

بک‌اند `IRR` (ریال) می‌فرستد ولی **کاربر تومان می‌بیند**.

**قاعده:** تبدیل فقط در لایه‌ی نمایش انجام شود (تقسیم بر ۱۰)،
و هر چه به بک‌اند فرستاده می‌شود **ریال** بماند.

> ⚠️ خطر: اگر جایی این تبدیل جا بیفتد، کاربر مبلغ را **۱۰ برابر**
> اشتباه می‌بیند. باید یک تابع واحد (`formatToman`) ساخته شود و
> همه‌جا فقط از آن استفاده شود — نه تقسیم دستی در هر کامپوننت.

---

## ✅ پوشش فلو — همه‌ی کارهای بدون وابستگی تمام شد

اندازه‌گیری با اسکریپت روی `flow.dot` و `portal.dot`:

```
اندپوینت یکتا در دات‌ها : ۵۹
وصل‌شده در کد ما        : ۵۸
وصل‌نشده                : ۱  ← فقط GET /health (به UI ربطی ندارد)
پوشش                    : ۹۸.۳٪
```

**یعنی هر چیزی که در فلو هست و اندپوینت دارد، وصل شده است.**
هر آنچه باقی مانده واقعاً منتظر بک‌اند است، نه کار فرانت.

### صفحه‌هایی که هنوز داده‌ی نمونه دارند — همه به‌خاطر نبود اندپوینت

| صفحه | اندپوینت لازم |
|---|---|
| کاربران ادمین + پروفایل جامع | `GET/PATCH/DELETE /admin/users` · `audit-logs` |
| مدیریت مالی | تراکنش‌ها · صدور فاکتور |
| لایسنس‌ها (تب دوم فروش) | کل ماژول لایسنس |
| ابزارها و تنظیمات | ریلیز نوت · اعلان سیستمی |
| فاکتورهای کاربر | `GET /invoices` · `GET /invoices/{id}/pdf` |
| لاگ‌های کاربر | `GET /logs` |
| مدیریت سرویس | نامشخص — در فلو هم نیست |

---

## 🔧 بدهی فنی سمت فرانت

- `productService.mock.js` هنوز آرایه‌ی تودرتو برمی‌گرداند؛ باید با اسپک
  جدید تخت شود. `flattenPlans` در `usePlans` هر دو را تحمل می‌کند پس
  بلاکر نیست.
- `VITE_USE_MOCK_PRODUCTS` هنوز `true` است؛ با آماده شدن داده‌ی واقعی
  باید `false` شود.
- **فرم پلن مبلغ ندارد** پس قیمت با صفر ثبت می‌شود؛ قیمت‌گذاری واقعی
  هنگام صدور پیش‌فاکتور انجام می‌شود. اگر قرار است پلن قیمت پایه داشته
  باشد، فرم به فیلد مبلغ نیاز دارد.
- **فرض «یک محصول»** در ساخت پلن: `product_id` در مسیر اندپوینت است و
  فعلاً اولین محصول انتخاب می‌شود. با آمدن محصول دوم فرم به dropdown
  نیاز دارد.

---

# 📨 متن آماده برای بک‌اند (بعد از دریافت دات‌ها)

> دات‌های فلو رو دیدم، خیلی چیزها رو روشن کرد. چند تا سؤال:
>
> **۱- نوع تحویل پلن.** توی فلو نوشتی تحویل از پلن مشخص می‌شه
> (install-by-us یا license-only) ولی `PlanOutput` همچین فیلدی نداره.
> فرانت از کجا بفهمه بعد از خرید باید تیکت نصب نشون بده یا کد لایسنس؟
>
> **۲- فاکتور و پیش‌فاکتور.** هر دو دات می‌گن این‌ها توی داشبورد کاربر
> دیده می‌شن، ولی خودت توی `flow.dot` نوشتی «not exposed in openapi».
> قراره اندپوینت بگیرن؟ صفحه‌ی فاکتور ما آماده‌ست ولی داده نداره.
>
> **۳- `ticket_id` هنوز متناقضه.** توی `LinkOrderTicket` عدده
> (integer) ولی `OrderTicketLinkOutput` و بقیه‌ی تیکتینگ UUID.
> کدوم درسته؟
>
> **۴- تغییر رمز.** `POST /auth/password/change` هنوز `current_password`
> نمی‌گیره. یعنی با فقط داشتن توکن میشه رمز رو عوض کرد. این امنیتیه.
>
> **۵- ارسال همگانی اعلان.** `recipient_ids` توی اسپک اجباریه
> (minItems=1) ولی توضیح `broadcast` می‌گه وقتی true باشه نادیده
> گرفته می‌شه. برای ارسال به همه دقیقاً چی بفرستم؟

## سؤال از کارفرما (نه بک‌اند)

> صفحه‌ی «مدیریت سرویس» که اولین آیتم داشبورد کاربره، توی هیچ‌کدوم از
> دو تا فایل فلو نیست. این صفحه قراره چی نشون بده؟
