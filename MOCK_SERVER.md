# سرور تقلبی بک‌اند — راهنمای تست

برای وقتی که بک‌اند واقعی در دسترس نیست (پشت VPN یا بالا نیامده).

## چرا

بک‌اند پشت فورتی‌کلاینت است و وقتی به VPN وصل می‌شویم دسترسی به
بیرون قطع می‌شود — یعنی نمی‌شود همزمان با کسی که بیرون است تست کرد.
این سرور همان قراردادِ `openapi.json` را روی همین کامپیوتر پیاده
می‌کند تا فرانت بدون هیچ وابستگی‌ای قابل تست باشد.

بعلاوه کارهایی می‌کند که با بک‌اند واقعی سخت است: مثلاً «الان ۵۰۰
بده» تا ببینیم UI چه واکنشی نشان می‌دهد.

## اجرا

دو ترمینال لازم است.

**ترمینال ۱ — سرور تقلبی:**

```bash
node mock-server.mjs
```

**ترمینال ۲ — فرانت:**

اول `.env` را عوض کنید:

```
VITE_API_BASE_URL=http://localhost:5000
VITE_USE_MOCK_PRODUCTS=false
```

بعد:

```bash
npm run dev
```

> ⚠️ بعد از تغییر `.env` حتماً `npm run dev` را ری‌استارت کنید —
> Vite متغیرها را فقط موقع بالا آمدن می‌خواند.
>
> ⚠️ بعد از تست، `.env` را به آدرس واقعی برگردانید.

## نکته‌های مهم

- **کد OTP همیشه `111111` است.**
- کاربر تازه‌ثبت‌نام‌کرده `is_verified=false` است — دقیقاً مثل واقعی.
- `POST /orders/` برای کاربر تأییدنشده ۴۰۳ می‌دهد، همان‌طور که
  بک‌اند واقعی باید بدهد.
- پاسخ لاگین عمداً `is_verified` **ندارد** (چون اسپک هم ندارد)، تا
  مطمئن شویم فرانت آن را از `/auth/me` می‌گیرد.
- داده در حافظه است؛ با بستن سرور پاک می‌شود.

## تست حالت‌های خطا

آدرس زیر را در مرورگر باز کنید تا **درخواست بعدیِ** آن مسیر با خطا
جواب بگیرد:

```
http://localhost:5000/__mock/fail/auth/login/401
http://localhost:5000/__mock/fail/orders//500
```

پاک کردن همه‌ی داده و خطاها:

```
http://localhost:5000/__mock/reset
```

## یک مسیر کامل برای تست دستی

1. `/register` → ثبت‌نام (کد OTP: `111111`)
2. بعد از ثبت‌نام، برو `/products/buy/new`
   → باید به `/dashboard/account` پرت شوی با پیام «ابتدا هویت خود
   را تکمیل کنید»
3. فرم تکمیل هویت را پر کن (کد ملی: هر ۱۰ رقمی مثل `0012345678`)
4. دوباره برو `/products/buy/new` → حالا باید باز شود
5. سفارش ثبت کن، برو `/products/buy/orders` → باید در فهرست باشد

## ⚠️ این فایل هرگز نباید در production استفاده شود

فقط ابزار توسعه است. احراز هویت واقعی ندارد و داده را جدی نمی‌گیرد.
