# کامپوننت‌های مشترک (هر سه صفحه)

> جهت‌ها RTL هستند: «راست» = start، «چپ» = end.

---

## 1. Header — ارتفاع 72px

- پس‌زمینه `--bg`، بوردر پایین `1px solid --divider`. sticky top.
- داخل container 1312، `display:flex; align-items:center; justify-content:space-between`.

| بخش | موقعیت | جزئیات |
|---|---|---|
| لوگو | راست | لوگوی «NG / CORION»، ابعاد ≈ 86 × 44. «NG» درشت و «CORION» زیر آن. (فایل SVG لوگو را از کارفرما بگیر) |
| منو | وسط | آیتم‌ها به ترتیب از راست: `خانه` · `محصول` · `خدمات` · `منابع` · `درباره ما` · `تماس با ما` — فونت nav (15/500)، رنگ `--text-2`، gap بین آیتم‌ها 48px |
| آیتم فعال | | رنگ `--text`، یک خط 2px به رنگ `--primary` در لبه پایین هدر، عرض = عرض متن + 16px |
| اکشن‌ها | چپ | دکمه Primary «درخواست دمو» 136×44 ، سپس (سمت چپ‌تر) Icon button منو 44×44، gap 16 |

---

## 2. Breadcrumb

- متن 14/24، رنگ `--text-muted`، جداکننده ` / ` با فاصله 8px.
- آیتم جاری رنگ `--link`.
- فاصله تا h1: 24px.

---

## 3. Section Header (عنوان سکشن)

دو حالت:
- **راست‌چین** (منابع، قوانین): h2 24/36 + زیرعنوان small (14/24، `--text-muted`)، فاصله 4px. در صورت وجود، لینک «مشاهده همه ←» در سمت چپ همان ردیف (14، `--link`، آیکون `ArrowLeft` 14px).
- **وسط‌چین** (خدمات): h2 28/40 + زیرعنوان 14/24، فاصله 8px.

---

## 4. CTA Box (باکس «سؤال دارید؟»)

در هر سه صفحه با محتوای متفاوت تکرار شده. یک کامپوننت با props بساز.

| ویژگی | مقدار |
|---|---|
| ارتفاع | 144px (منابع، در ستون) · 240px (قوانین) · 216px (خدمات) |
| padding | 32px 40px |
| پس‌زمینه | `linear-gradient(90deg, #031A40 0%, #010F29 60%)` + `--glow-md` |
| بوردر / radius | `1px solid --border-glow` / 12px |
| تصویر | آیکون نئونی «حباب گفتگو + ؟» ≈ 120–160px (asset: `assets-placeholder/cta-question.png`) |
| عنوان | 22/32/700 سفید |
| توضیح | 14/24 `--text-2`، max-width 460 |
| دکمه‌ها | Primary + (اختیاری) Outline، gap 16، فاصله از متن 24 |

جای تصویر: **منابع** → سمت چپ · **قوانین** و **خدمات** → سمت راست (کنار عنوان).

---

## 5. Customers (مشتریان ما)

- padding-y 48، عنوان h2 وسط‌چین «مشتریان ما» (24/36).
- یک ردیف 6 لوگو، `justify-content: space-around`، فاصله عنوان تا لوگوها 32.
- هر آیتم: لوگو 64×64 (grayscale، opacity 0.7، hover → 1) + کپشن 12/20 `--text-muted` با فاصله 8.
- لوگوها از راست:
  1. سازمان فناوری اطلاعات ایران
  2. شرکت مخابرات ایران
  3. بانک صادرات ایران
  4. [نام ناخوانا — از کارفرما بپرس]
  5. شرکت ملی نفت ایران
  6. بانک ملت

> لوگوها قابل بازسازی نیستند؛ فایل اصلی را از کارفرما بگیر.

---

## 6. Footer — ارتفاع ≈ 308px

- بوردر بالا `1px solid --divider`، padding-top 48.
- ردیف اصلی: 4 ستون (از راست به چپ):

| ستون | عرض تقریبی | محتوا |
|---|---|---|
| ۱. «با ما در ارتباط باشید» | 240 | عنوان 15/700 با یک خط عمودی 2×16 آبی قبل از آن. زیرش 4 آیکون‌باکس 36×36 (radius 6، bg `--surface-2`، gap 12) از راست: `Mail` · `Youtube` · `Send`(تلگرام) · `Linkedin` |
| ۲. «قوانین» | 200 | لینک‌ها: حریم خصوصی · قوانین و مقررات · شرایط مجوز |
| ۳. «دسترسی سریع» | 200 | لینک‌ها: محصول · خدمات · منابع · درباره ما · تماس با ما |
| ۴. برند (چپ‌ترین) | 300 | لوگو 108×56 · تگ‌لاین «زیرساخت امن، آینده مطمئن» (14، `--link`) · توضیح: «NGCorion یک پلتفرم یکپارچه برای مدیریت، ارزیابی و ارتقای امنیت زیرساخت‌های فناوری اطلاعات در سازمان‌ها است.» (14/24، `--text-muted`، max-width 290) |

- عنوان ستون‌ها: 15/24/700 سفید، فاصله تا لینک‌ها 20.
- لینک‌ها: 14/24 `--text-2`، فاصله بین لینک‌ها 6 (ریتم عمودی 30px).
- فاصله ستون ۱ تا ۲ ≈ 180، ۲ تا ۳ ≈ 100؛ ستون برند به لبه چپ چسبیده.
- **Bottom bar**: margin-top 32، بوردر بالا `--divider`، ارتفاع 64، flex space-between:
  - راست: «© ۱۴۰۵ NGCorion. تمامی حقوق محفوظ است.» (13، `--text-muted`)
  - چپ: «سیاست کوکی‌ها  |  نقشه سایت» (13، `--text-2`)
