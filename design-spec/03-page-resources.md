# صفحه «مرکز منابع» — `/resources`

> مرجع: `reference/resources.jpg` (دقیق‌ترین تصویر، 1px تصویر = 2px طراحی) · ارتفاع کل صفحه ≈ **2160px**
> وایرفریم: `wireframes/resources.svg`

| # | سکشن | y شروع | ارتفاع |
|---|---|---|---|
| 0 | Header | 0 | 72 |
| 1 | Hero | 72 | 400 |
| 2 | دسته‌بندی منابع | 472 | 568 |
| 3 | منابع پیشنهادی | 1040 | 312 |
| 4 | آخرین به‌روزرسانی‌ها + منابع پرطرفدار + CTA | 1352 | 492 |
| 5 | Footer | 1852 | 308 |

منوی فعال هدر: **منابع**.

---

## 1. Hero — 1440 × 400

- بوردر پایین `1px solid --divider`.
- دو ستون: **متن (راست) عرض 520** · **تصویر (چپ) ≈ 770 × 380**.
- ستون متن (padding-top 48):
  - Breadcrumb: `خانه / منابع`
  - h1 (40/56/800): «مرکز منابع NGCorion» — margin-top 24
  - lead (18/32، `--text-2`): «به مستندات، راهنماها، مقالات، نسخه‌ها و منابع موردنیاز برای استفاده بهتر از NGCorion دسترسی داشته باشید.» — margin-top 16
  - **Search box**: 520 × 56، margin-top 24، bg `#011734`، بوردر `1px solid --border-glow`، radius 8، آیکون `Search` 24px در راست (padding 20)، placeholder: «جستجو در **مستندات**، مقالات، راهنماها و …» (15، `--text-muted`)
  - **Tags row** (margin-top 16، flex، gap 8): برچسب «موضوعات پربحث:» (13، `--text-muted`) سپس چیپ‌ها:
    `نصب و راه‌اندازی` (فعال) · `امنیت` · `نسخه‌ها` · `API` · `پشتیبان‌گیری`
    - چیپ: ارتفاع 28، padding-x 12، radius 6، bg `--surface-2`، متن 12 `--text-2`. فعال: بوردر `--border-glow` + متن سفید.
- تصویر: ایزومتریک «NG» با 6 کارت شناور (مستندات فنی، مقالات و راهنماها، پایگاه دانش، اطلاعیه‌های نسخه‌ها، آموزش‌ها، دانلودها) → **asset**: `assets-placeholder/hero-resources.png` (موقت)

---

## 2. دسته‌بندی منابع

- Section header (راست‌چین): «دسته‌بندی منابع» / «به‌سرعت به بخش موردنظر خود دسترسی پیدا کنید.»
- **گرید 3 ستون × 2 ردیف**، کارت **421 × 208**، gap 24.
- کارت: وسط‌چین، padding 24، radius 8، استایل کارت نئونی (tokens). آیکون `ChevronLeft` 16px (`--text-muted`) گوشه بالا-چپ (top 16، left 16).
  - آیکون 40px (glow) → فاصله 16 → h3 18/28 → فاصله 8 → توضیح 14/24 `--text-2` (max-width 260، دو خط) → فاصله 16 → لینک 14 `--link` با `←`.
- کارت اول (مستندات) حالت **active/hover**: bg `--surface-active` + `--glow-lg` + بوردر `--primary`.

ترتیب (از راست، ردیف به ردیف):

| # | آیکون (lucide) | عنوان | توضیح | لینک |
|---|---|---|---|---|
| 1 | `FileText` | مستندات | راهنمای جامع استفاده، پیکربندی و مدیریت NGCorion. | مشاهده مستندات ← |
| 2 | `Rocket` | شروع کار | راهنمای گام‌به‌گام برای شروع سریع و آسان. | مشاهده راهنما ← |
| 3 | `BookOpen` | پایگاه دانش | پاسخ‌ها و راهکارهای عملی به سوالات متداول. | مشاهده مقالات ← |
| 4 | `ShieldCheck` | امنیت و انطباق | منابع تخصصی امنیت زیرساخت و استانداردهای معتبر. | مشاهده منابع ← |
| 5 | `RefreshCw` | نسخه‌ها | اطلاع از جدیدترین نسخه‌ها و تغییرات محصول. | مشاهده نسخه‌ها ← |
| 6 | `CloudDownload` | دانلودها | فایل‌ها، بروشورها و منابع قابل دانلود NGCorion. | مشاهده دانلودها ← |

---

## 3. منابع پیشنهادی

- Section header: «منابع پیشنهادی» / «مهم‌ترین و پرکاربردترین منابع برای شروع کار یا عمیق‌تر شدن در موضوعات مختلف.» + لینک چپ: «مشاهده همه منابع پیشنهادی ←»
- **گرید 4 ستون**، کارت **310 × 192**، gap 24، padding 20، text-align راست.
- ساختار کارت (بالا به پایین):
  - ردیف بالا: آیکون‌باکس 32×32 (radius 6، bg `--surface-2`، آیکون 18) + کنارش بج دسته (12، `--link`، بدون بک‌گراند)، gap 8
  - h3 16/26/700 — margin-top 16
  - توضیح 14/22 `--text-muted` — margin-top 6
  - ردیف پایین (absolute bottom 20): راست «PDF ۱۲ صفحه» (12، `--text-muted`) · چپ آیکون `Download` 18 `--icon`

| # | آیکون | بج | عنوان | توضیح | متا |
|---|---|---|---|---|---|
| 1 | `FileText` | مستندات | راهنمای نصب و راه‌اندازی | مراحل نصب و پیکربندی اولیه NGCorion | PDF ۱۲ صفحه |
| 2 | `Clock` | راهنما | شروع سریع | از نصب تا اولین ارزیابی امنیتی | PDF ۸ صفحه |
| 3 | `Target` | امنیت | بهترین روش‌های Hardening | نکات کلیدی برای ایمن‌سازی دارایی‌ها | PDF ۱۰ صفحه |
| 4 | `Package` | معرفی محصول | معرفی قابلیت‌های NGCorion | نمای کلی محصول و کاربردها | PDF ۱۴ صفحه |

- زیر این سکشن یک خط جداکننده افقی `--divider` (margin-top 24).

---

## 4. آخرین به‌روزرسانی‌ها + منابع پرطرفدار

لی‌آوت دو ستونه: **ستون اصلی 968px** (چپ) · **سایدبار 320px** (راست) · gap 24.
توجه: در RTL سایدبار «منابع پرطرفدار» **سمت راست** است و جدول سمت چپ.

### 4.a سایدبار «منابع پرطرفدار» — 320 × 344

- باکس: bg `--surface`، بوردر `--border`، radius 12، padding 24.
- عنوان: آیکون `Flame` 20 (آبی) + «منابع پرطرفدار» 18/700. فاصله تا لیست 24.
- 6 ردیف، ریتم 46px: باکس شماره 32×28 (radius 6، bg `--surface-2`، متن 13 `--link`) + عنوان 14 `--text-2`، gap 12.
  1. راهنمای نصب و راه‌اندازی
  2. سوالات متداول (FAQ)
  3. مستندات API
  4. چک‌لیست پیش‌نیازها
  5. راهنمای Hardening
  6. لیست دارایی‌های پشتیبانی‌شده

### 4.b جدول «آخرین به‌روزرسانی‌ها» — 968 × 260

- عنوان h2 «آخرین به‌روزرسانی‌ها» + لینک چپ «مشاهده همه ←». فاصله تا جدول 16.
- باکس: bg `--surface`، بوردر `--border`، radius 8، padding-x 16.
- 5 ردیف × **52px**، بین ردیف‌ها بوردر `1px --border`.
- ستون‌ها (از راست):

| ستون | عرض | استایل |
|---|---|---|
| بج دسته | 104 × 32 | radius 6، متن 12 وسط. آبی: bg `--surface-2` متن `--link` · خاکستری: bg `--badge-gray` متن سفید |
| عنوان | 300 | 14/700 سفید |
| توضیح | 280 | 13 `--text-muted` |
| تاریخ | 100 | 13 `--text-2`، اعداد فارسی |
| آیکون | 24 | `Circle` کوچک 14 (یا `ChevronLeft`) `--text-muted` |

| بج (رنگ) | عنوان | توضیح | تاریخ |
|---|---|---|---|
| نسخه‌ها (آبی) | NGCorion v2.3.0 منتشر شد | قابلیت‌های جدید، بهبودها و رفع مشکلات | ۱۴۰۵/۰۶/۱۲ |
| پایگاه دانش (آبی) | رفع مشکل ارتباط با تجهیزات Cisco | بررسی دلایل و راهکارهای رفع خطا | ۱۴۰۵/۰۶/۱۰ |
| مستندات (آبی) | راهنمای تنظیم دسترسی کاربران | آموزش کامل مدیریت کاربران و نقش‌ها | ۱۴۰۵/۰۶/۰۸ |
| امنیت (خاکستری) | استفاده از CIS Benchmarks در NGCorion | راهنمای استفاده از Baselineهای امنیتی | ۱۴۰۵/۰۶/۰۵ |
| دانلودها (خاکستری) | چک‌لیست پیش‌نیازهای استقرار | بررسی الزامات سخت‌افزاری و نرم‌افزاری | ۱۴۰۵/۰۶/۰۱ |

### 4.c CTA (زیر جدول، در همان ستون 968) — 968 × 132، margin-top 24

- راست: عنوان «پاسخ سؤال خود را پیدا نکردید؟» (20/700) + توضیح «اگر در منابع موجود به پاسخ موردنظر خود نرسیدید، با تیم ما ارتباط باشید.» (13، max-width 300)
- وسط: دکمه Primary «ارسال درخواست پشتیبانی» 172×44 + Outline «تماس با ما ←» 136×44، gap 12
- چپ: آیکون نئونی سؤال ≈ 110px

---

## 5. Footer

طبق `01-shared-components.md`. (این صفحه سکشن «مشتریان ما» ندارد.)
